# CarDiagnostik / AutoDiag AI — backend

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.0

# Vector memory (optional; skip on Python 3.13+)
chromadb>=0.5.0; python_version < "3.13"

pytest>=8.0
