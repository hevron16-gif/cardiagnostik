<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CarDiagnosticApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.SegmentedControl;assembly=Syncfusion.Maui.Toolkit"
    xmlns:pages="clr-namespace:CarDiagnosticApp.Pages"
    Shell.FlyoutBehavior="Flyout"
    Title="CarDiagnosticApp">

    <ShellContent
        Title="Dashboard"
        Icon="{StaticResource IconDashboard}"
        ContentTemplate="{DataTemplate pages:MainPage}"
        Route="main" />

    <ShellContent
        Title="Projects"
        Icon="{StaticResource IconProjects}"
        ContentTemplate="{DataTemplate pages:ProjectListPage}"
        Route="projects" />

    <ShellContent
        Title="Manage Meta"
        Icon="{StaticResource IconMeta}"
        ContentTemplate="{DataTemplate pages:ManageMetaPage}"
        Route="manage" />

     <ShellContent
         Title="История"
         Icon="{StaticResource IconHistory}"
         ContentTemplate="{DataTemplate pages:HistoryPage}"
         Route="history" />

     <ShellContent
         Title="База знаний"
         Icon="{StaticResource IconKnowledge}"
         ContentTemplate="{DataTemplate pages:KnowledgePage}"
         Route="knowledge" />

     <ShellContent
         Title="Live Data"
         Icon="{StaticResource IconDashboard}"
         ContentTemplate="{DataTemplate pages:LiveDataPage}"
         Route="livedata" />

     <Shell.FlyoutFooter>
        <Grid Padding="15">
            <sf:SfSegmentedControl x:Name="ThemeSegmentedControl" 
                VerticalOptions="Center" HorizontalOptions="Center" SelectionChanged="SfSegmentedControl_SelectionChanged"
                SegmentWidth="40" SegmentHeight="40">
                <sf:SfSegmentedControl.ItemsSource>
                    <x:Array Type="{x:Type sf:SfSegmentItem}">
                        <sf:SfSegmentItem ImageSource="{StaticResource IconLight}" SemanticProperties.Description="Light mode"/>
                        <sf:SfSegmentItem ImageSource="{StaticResource IconDark}" SemanticProperties.Description="Dark mode"/>
                    </x:Array>
                </sf:SfSegmentedControl.ItemsSource>
            </sf:SfSegmentedControl>
        </Grid>
    </Shell.FlyoutFooter>

</Shell>
